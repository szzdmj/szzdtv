// Centralized repositories and module includes (compatible with Gradle 5.x / 6.x style).
pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    // If you prefer to fail on project repositories, keep this; for Gradle 5.x this block is not required.
    // For compatibility with the original project structure we leave module repositories to root buildscript.
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // or FAIL_ON_PROJECT_REPOS on newer Gradle
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
        // Crosswalk repo (optional)
        // maven { url 'https://download.01.org/crosswalk/releases/crosswalk/android/maven2' }
    }
}

include ':smartyoutubetv', ':browser', ':exoplayeractivity', ':appupdatechecker', ':common'

def rootDir = settingsDir

// prepare to git submodule (keep original SharedModules integration)
gradle.ext.sharedModulesRoot = new File(rootDir, '../SharedModules').exists() ? new File(rootDir, '../SharedModules') : new File(rootDir, './SharedModules')
apply from: new File(gradle.ext.sharedModulesRoot, 'core_settings.gradle')

gradle.ext.sharedModulesConstants = new File(gradle.ext.sharedModulesRoot, 'constants.gradle')

// prepare to git submodule for MediaServiceCore
gradle.ext.mediaServiceCoreRoot = new File(rootDir, '../MediaServiceCore').exists() ? new File(rootDir, '../MediaServiceCore') : new File(rootDir, './MediaServiceCore')
apply from: new File(gradle.ext.mediaServiceCoreRoot, 'core_settings.gradle')
