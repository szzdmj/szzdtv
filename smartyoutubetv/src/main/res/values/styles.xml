<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal AppCompat theme for PlayerActivity / AppCompatActivity usage -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize colors/attributes if needed -->
        <item name="android:windowBackground">@color/youtube_background</item>

        <!-- completely disable button sounds -->
        <item name="android:soundEffectsEnabled">false</item>

        <!-- Disable window content transitions / default window animations to avoid
             loading animator resources as layouts during setContentView -->
        <item name="android:windowContentTransitions">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="AppTheme.Dark" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/youtube_background_high_contrast</item>
        <item name="android:soundEffectsEnabled">false</item>
        <item name="android:windowContentTransitions">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="buttonStyle">
        <item name="android:layout_marginLeft">10dip</item>
        <item name="android:layout_marginRight">10dip</item>
        <item name="android:layout_marginTop">10dip</item>
        <item name="android:layout_marginBottom">10dip</item>
    </style>
</resources>
